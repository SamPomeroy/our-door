// scaffold only — no log display logic yet
export default function AdminDashboard() {
  return (
    <div>
      <h1>Admin Dashboard</h1>
      {/* TODO: fetch + display log entries via getLogs() from api.js */}
    </div>
  );
}
