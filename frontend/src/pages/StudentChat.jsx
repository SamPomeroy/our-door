// scaffold only — no chat logic yet
export default function StudentChat() {
  return (
    <div>
      <h1>Student Chat</h1>
      {/* TODO: message input + response display, calls sendMessage() from api.js */}
    </div>
  );
}
