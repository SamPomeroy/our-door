// scaffold only — no auth logic yet
export default function Login() {
  return (
    <div>
      <h1>Login</h1>
      {/* TODO: password input + role select (student|admin), calls login() from api.js */}
    </div>
  );
}
